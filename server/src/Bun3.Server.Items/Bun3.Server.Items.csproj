<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.1</TargetFramework>
    <LangVersion>9.0</LangVersion>
    <Nullable>enable</Nullable>
    <RootNamespace>Bun3.Server.Items</RootNamespace>
    <PackageId>Bun3.Server.Items</PackageId>
    <Version>0.1.0</Version>
    <Authors>Bun3</Authors>
    <RepositoryUrl>https://github.com/Bun3/bun3-kit</RepositoryUrl>
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
    <Description>Server item framework — catalog (interning, indexes, regen metadata), unified stack/instance inventory (BigNum quantities, atomic transactions, change tracking), recipes, reward tables, regen, expiry, cross-inventory transfer, ActionLog ledger integration</Description>
  </PropertyGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="Bun3.Server.Tests" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Bun3.Server.Core\Bun3.Server.Core.csproj" />
    <ProjectReference Include="..\..\..\common\src\com.bun3.gameplay\Bun3.Gameplay.csproj" />
  </ItemGroup>

</Project>
